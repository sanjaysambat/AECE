# Copy to `backend/.env` if you prefer env-file-based local dev.
# Docker Compose in `../docker-compose.yml` also sets these.

DATABASE_URL=postgresql+psycopg://aece:aece@localhost:5432/aece
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
OPENAI_API_KEY=
LLM_MODEL=gpt-4.1-mini
AUTO_CREATE_TABLES=true

